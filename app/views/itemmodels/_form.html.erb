<%= form_with(model: itemmodel, local: true) do |form| %>
  <% if itemmodel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(itemmodel.errors.count, "error") %> prohibited this itemmodel from being saved:</h2>

      <ul>
      <% itemmodel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
